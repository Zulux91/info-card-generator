INSTRUCCIONES PARA WINDOWS:

1. Abre una terminal (CMD o PowerShell) dentro de esta carpeta.
2. Ejecuta:

   npm install

3. Luego ejecuta:

   npm run dev

4. Abre tu navegador en: http://localhost:5173

NOTAS:
- Si necesitas borrar node_modules y estás en PowerShell, usa:

      rd /s /q node_modules
      del package-lock.json

- Si estás en CMD, igual.
- Recuerda usar `npx tailwindcss.cmd init -p` en lugar de `npx tailwindcss init -p` si hicieras la instalación manual.

Este proyecto está listo para usarse.
